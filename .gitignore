# Runtime artifacts
.venv/
web/node_modules/
__pycache__/
*.pyc
*.pyo

# User data (preserved across updates)
data/
logs/

# Build artifact — INCLUDED in releases, NOT gitignored
# web/dist/

# Project-only artifacts (NOT on GitHub)
AGENTS.md
.codex/
android-web-client/
tools/
screenshot/
.user-logs/
reference.d/
.claude/

# Local Hermes agent data (plans, memory) — NOT project code
.hermes/

# Nested .release copies — the canonical worktree is this repo root; nested copies are build/runtime junk
.release/

# Tests (local only)
web/e2e/

# OS
.DS_Store
Thumbs.db
